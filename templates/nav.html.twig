<nav class="p-5 navbar navbar-expand-lg navbar-light bg-light">
	{% if app.user %}
		<span class="mb-5 mr-5">{{ app.user.firstName }}
			{{app.user.LastName}}</span>
	{% endif %}
	<div class="container-fluid">
		<a class="mr-5 fs-1 navbar-brand text-white-50 mb-2" href="{{path('app_blog')}}">Olfragrance</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link" href="{{path('app_blog')}}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('app_profilage')}}">Profilage</a>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Articles</a>

					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item" href="{{path('app_information')}}">Où se mettre du parfum ?</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{path('app_information1')}}">Eau de Toilette ou Eau de Parfum ?</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{path('app_information2')}}">Note de tête, de cœur, de fond ?</a>
						</li>
						
					</ul>
					
				</li>

				{% if app.user %}

					{% else %}
						<li class="nav-item">
							<a class="nav-link" href="{{path('app_login')}}">Connexion</a>
						</li>

				{% endif %}

			</ul>
			{% if app.user %}
				<div class="token-user">
					<div class="d-flex nav-item ">
						<a href="{{ path('app_classement') }}" class="nav-link text-dark">Mon compte
						</a>
						<a href="{{ path('app_logout') }}" class="nav-link text-dark">Déconnexion</a>
					</div>
				</div>
			{% endif %}
			{# <form class="d-flex">
				<input class="form-control me-2 my-2" type="search" placeholder="Search" aria-label="Search">
				<button class="btn btn-outline-white" type="submit">Search</button>
			</form> #}


		</div>
	</div>


</nav>
